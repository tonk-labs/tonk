{
  "name": "@tonk/sprinkles",
  "version": "0.0.1",
  "description": "Unified dev environment for Tonk - runs host-web and relay together",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm run build:wasm && pnpm run build:sw",
    "build:wasm": "mkdir -p ../core-js/dist && cd ../core && pnpm run build:browser && cp -r pkg-browser/* ../core-js/dist/ && cd ../core-js && pnpm run embed-wasm && pnpm run build:wrapper",
    "build:sw": "cd ../host-web && NODE_ENV=development pnpm run build:sw && pnpm run copy-sw-for-dev",
    "dev": "mprocs",
    "dev:relay": "cd node_modules/@tonk/relay && pnpm run dev",
    "dev:host-web": "cd node_modules/@tonk/host-web && PORT=4000 NODE_ENV=development pnpm run dev"
  },
  "dependencies": {
    "@tonk/core": "workspace:*",
    "@tonk/host-web": "workspace:*",
    "@tonk/relay": "workspace:*"
  },
  "devDependencies": {
    "mprocs": "^0.7.1",
    "wait-port": "^1.1.0"
  }
}
