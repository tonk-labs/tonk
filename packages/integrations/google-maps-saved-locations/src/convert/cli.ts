import * as dotenv from "dotenv";
import { convertLocations } from "./convertLocations";

dotenv.config();

// This file is now just a placeholder for the CLI functionality
// The actual command registration and parsing is done in bin.ts