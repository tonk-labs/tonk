procs:
  launcher:
    shell: cd ../launcher && NODE_ENV=develoment bun run dev

  desktonk:
    shell: mkdir -p automerge-repo-data && rm -rf automerge-repo-data && NODE_ENV=develoment bunx vite

  relay:
    shell: bun scripts/run-relay.ts
