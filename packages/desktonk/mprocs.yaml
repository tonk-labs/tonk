procs:
  launcher:
    shell: cd ../launcher && NODE_ENV=develoment bun run dev

  desktonk:
    shell: rm -rf automerge-repo-data && NODE_ENV=develoment bunx vite

  relay:
    shell: bun scripts/run-relay.ts
