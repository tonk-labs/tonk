// YAML language pack. Loaded lazily by `<tonk-code mode="yaml">`
// from `./tonk-code-lang-yaml.js`. The element imports the
// default export and reconfigures its language compartment with
// the returned `LanguageSupport`.

import { yaml } from "@codemirror/lang-yaml";

export default yaml();
