[package]
name = "tonk-host"
version.workspace = true
authors.workspace = true
edition.workspace = true
description = "<tonk-host>, <tonk-repository>, <tonk-branch> — IO ownership and routing context for tonk custom elements."
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
bytes = { workspace = true }
custom-elements = { workspace = true }
futures = { workspace = true }
js-sys = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde-wasm-bindgen = { workspace = true }
serde_ipld_dagjson = { workspace = true }
serde_json = { workspace = true }
tonk-schema = { workspace = true }
wasm-bindgen = { workspace = true }
wasm-bindgen-futures = { workspace = true }
wasm-streams = { workspace = true }
web-sys = { workspace = true, features = [
  "AbortController",
  "AbortSignal",
  "CustomElementRegistry",
  "CustomEvent",
  "CustomEventInit",
  "Document",
  "Element",
  "Event",
  "EventInit",
  "EventTarget",
  "Headers",
  "HtmlElement",
  "Node",
  "ReadableStream",
  "ReadableStreamDefaultReader",
  "Request",
  "RequestInit",
  "Response",
  "Window",
] }

[dev-dependencies]
dialog-common = { workspace = true, features = ["helpers"] }
wasm-bindgen-test = { workspace = true }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
  "cfg(dialog_test_native_integration)",
  "cfg(dialog_test_wasm_integration)",
  'cfg(feature, values("web-integration-tests"))',
] }
