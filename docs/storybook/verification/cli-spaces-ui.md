# Verification: CLI, spaces, and browser runtime

Use a built CLI with isolated native state, deterministic fake remotes, and at
least two independent repository/account actors. TTY rows need a real
pseudoterminal. Browser rows use real Chrome with a clean profile and a coherent
deployed build. Record subject IDs and branch heads, not only display names.

## Shared CLI contract

| ID | P | Condition | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `CLI-01` | P2 | TTY + pipe | Every command/subcommand is reachable through help and mapped to a journey ([Inventory](../cli/command-surface.md#complete-command-inventory)). | Generate clap command tree from current binary. | 1. Run root/help/all/guides.<br>2. Run help for every visible/hidden command.<br>3. compare catalog. | All current entries appear once in command inventory; known help exits zero/no state; unknown help is actionable. | — |
| `CLI-02` | P1 | cli | Help and usage errors create no profile/site/lock/telemetry mutation ([Exit early](../cli/command-surface.md#exit-early)). | Empty isolated home; snapshot filesystem. | 1. Run no-command/help/version and representative invalid argv per family.<br>2. diff filesystem/network. | Only documented benign state, if any; no profile/site/account/remote; no network; stable exit/streams. | — |
| `CLI-03` | P1 | cli | Space selection precedence is flag, environment, nearest binding, then none ([Resolve](../cli/command-surface.md#resolve)). | Three distinct spaces; parent/nested bindings; env fourth name. | 1. run read under each combination.<br>2. use missing/stale names.<br>3. inspect bindings. | Exact precedence; explicit selection does not rewrite binding; stale/missing errors name source. | — |
| `CLI-04` | P2 | pipe | Human/JSON/notation/CSV/HTML/blob stdout stays free of diagnostics ([Interactions](../cli/command-surface.md#interactions-with-other-systems)). | Trigger update warning, sync warning, and verbose error around each output type. | 1. capture stdout/stderr.<br>2. parse promised format/bytes. | stdout parses/is byte-identical; warnings/chains only stderr; exit code matches result. | — |
| `CLI-05` | P2 | cli | `--verbose` expands the chain without changing product result ([Resolve](../cli/command-surface.md#resolve)). | Nested runtime failure fixture. | 1. run default and verbose.<br>2. compare code/stdout/state. | Same code/stdout/state; concise outer message versus full ordered stderr chain; no secrets. | — |
| `CLI-06` | P1 | cli + offline | Dry-run never commits or touches remote ([Exit early](../cli/command-surface.md#exit-early)). | Upstream configured; fixtures for eval/assert/retract/concept/view/home/blob/import/agents. | 1. run each dry-run.<br>2. inspect branch/site/blob/remote. | Planned result/matches; zero local claims/ref changes/blob ingest and zero remote requests. | — |
| `CLI-07` | P1 | cli + fault | Local commit followed by push failure reports `committed-local` and retries safely ([Settle](../cli/command-surface.md#settle)). | Upstream; fail post-push after a representative write in each write family. | 1. commit write.<br>2. fail push.<br>3. restart status/query.<br>4. push/retry. | Local data appears once, branch ahead, warning on stderr; later push syncs; rerun is no-op or intended supersede. | — |
| `CLI-08` | P1 | cli + fault | Lost remote response is reconciled before mutation replay ([Recovery rules](../cross-cutting/failure-and-recovery.md#recovery-rules)). | Drop response after push/join/invite shortcut/space link commit. | 1. run mutation.<br>2. drop response.<br>3. restart/status/retry. | Unknown outcome stated; status identifies committed state; retry does not duplicate authority/registration/commit. | — |
| `CLI-09` | P1 | pipe + restart | Broken stdout after commit cannot cause mutation replay ([Edge cases](../cli/command-surface.md#edge-cases)). | Close stdout at readiness barrier after representative local/remote commit. | 1. run mutating command.<br>2. break pipe.<br>3. inspect with new process.<br>4. retry. | Output error may be non-zero; durable mutation occurs once and is discoverable; safe retry. | — |
| `CLI-10` | P1 | restart | SIGINT/SIGTERM at each command-family boundary has explicit state ([Cancel and interrupt](../cli/command-surface.md#cancel-and-interrupt)). | Barriers before/after local transaction, remote send/accept, output publish, migration/update swap. | 1. signal at each barrier.<br>2. restart and inspect/recover. | Before-boundary unchanged; after-boundary exact checkpoint; no stale locks/temp files or silent partial success. | — |
| `CLI-11` | P2 | cli + locked/corrupt | Locked, read-only/full, malformed, and unsupported local state fail safely ([Error families](../cross-cutting/failure-and-recovery.md#error-families)). | Isolated fixtures for registry/profile/account/telemetry/update/output stores. | 1. run relevant read/write.<br>2. capture code/streams.<br>3. remove fault/retry. | Store named; no unrelated/remote mutation; malformed state not overwritten; retry succeeds after repair. | — |
| `CLI-12` | P1 | ci | CI discovers every intended CLI test, including native-only exceptions ([Open questions](../cli/command-surface.md#open-questions-and-verification)). | Current CI shell; list all tests and attributes. | 1. list tests.<br>2. compare 308 integration attributes and unit modules.<br>3. audit 22 direct tokio tests. | Every intended test discovered in its target; native-only tests justified; no silent cross-target omissions. | — |

## Space lifecycle, sync, and collaboration

| ID | P | Condition | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `SPACE-01` | P2 | cli | Space list distinguishes empty, registered, data-only, missing-data, and account-listed state ([Resolve](../spaces/lifecycle-and-collaboration.md#resolve)). | Seed each state plus duplicate labels/different subjects. | 1. run human/JSON list.<br>2. restart/repeat. | Every entry/state represented once with stable identity/schema; no network or mutation. | — |
| `SPACE-02` | P1 | restart | Local-only creation is atomic or recoverable at site/registry/binding checkpoints ([Cross a boundary](../spaces/lifecycle-and-collaboration.md#cross-a-boundary)). | Signed out; fault before/after site, repository, registry, binding writes. | 1. create at each fault.<br>2. restart list/use/adopt/rm.<br>3. retry same name. | One site/registration/binding or explicit adoptable/orphan cleanup; no invisible data or overwritten collision. | — |
| `SPACE-03` | P1 | offline + restart | Signed-in creation retains ownership locally while service is pending/unavailable ([Remain in flight](../spaces/lifecycle-and-collaboration.md#remain-in-flight)). | Active account with customer Registered, Suspended, Active/offline. | 1. create in each state.<br>2. write locally/restart.<br>3. activate/reconnect/sync. | Same subject/data survives; ownership/queued state explicit; one hosted/account entry after recovery. | — |
| `SPACE-04` | P1 | cli + locked/corrupt | `space new --site` adopts only a valid existing site and never overwrites ([Resolve](../spaces/lifecycle-and-collaboration.md#resolve)). | Valid unregistered site, registered site, ordinary directory, wrong repository identity, read-only/symlink path. | 1. attempt adoption/collision.<br>2. inspect paths/registry.<br>3. recover valid data-only site. | Valid site adopted once; every invalid/collision unchanged with exact error; no new repository replaces old data. | — |
| `SPACE-05` | P2 | cli | Use/unbind changes only exact selection pointers ([Settle](../spaces/lifecycle-and-collaboration.md#settle)). | Parent/nested/vanished absolute bindings. | 1. bind/use from parent/subdir.<br>2. unbind subdir/current/absolute vanished path.<br>3. inspect site/registry. | Exact precedence and match; data/registration untouched; subdirectory does not remove parent exact entry. | — |
| `SPACE-06` | P1 | TTY + restart | Local remove decline changes nothing; confirmed remove has recoverable checkpoints ([Cross a boundary](../spaces/lifecycle-and-collaboration.md#cross-a-boundary)). | Local-only and account-listed local replica; fault at binding/registry/data removal. | 1. decline.<br>2. confirm and fault each stage.<br>3. restart list/filesystem/recover. | Decline unchanged; confirmed path deletes intended scope or reports partial exact recovery; unrelated spaces intact. | — |
| `SPACE-07` | P1 | cli + restart | `--keep-data` unregisters without deleting and later adoption restores it ([Settle](../spaces/lifecycle-and-collaboration.md#settle)). | Canonical/custom registered sites with data. | 1. remove keep-data.<br>2. inspect list/path/name reservation.<br>3. adopt same site.<br>4. query data. | Data/subject unchanged; registration/binding removed then restored once; collisions safely rejected. | — |
| `SPACE-08` | P1 | fault + restart | Linking local-only to account is generation/subject-idempotent ([Cross a boundary](../spaces/lifecycle-and-collaboration.md#cross-a-boundary)). | Local space; active account; faults at authority/directory/hosting/upstream/push; customer variants. | 1. link/fault/restart at each stage.<br>2. run link/status/list.<br>3. inspect second device. | One owner/account entry/hosted subject/upstream; local data/binding unchanged; retry resumes exact subject or explains block. | — |
| `SPACE-09` | P1 | cli | Link refuses signed-out, joined, foreign-owned, and already-owned targets safely ([Exit early](../spaces/lifecycle-and-collaboration.md#exit-early)). | One fixture per relationship. | 1. run link.<br>2. inspect all authority/account/remote/local state. | No ownership transfer or remote write on invalid relationship; already-owned is idempotent/actionable. | — |
| `SPACE-10` | P1 | cli + offline | Account directory list/pull handles duplicates, offline cache, missing/stale and exact subjects ([Resolve](../spaces/lifecycle-and-collaboration.md#resolve)). | Duplicate names, stale deleted fact, existing local subject/name collision; provider blocked. | 1. list human/JSON.<br>2. pull by name/subject/custom slug.<br>3. restart. | Ambiguous name rejected; exact subject succeeds once; collision/data boundaries explicit; offline uses documented cache/error. | — |
| `SPACE-11` | P1 | restart | Account-space pull crash cannot leave invisible or duplicate replica ([Edge cases](../spaces/lifecycle-and-collaboration.md#edge-cases)). | Remote account space; faults after fetch/site/registry/binding. | 1. pull and terminate at each stage.<br>2. restart list/pull/adopt/query. | One exact replica or explicit adoptable checkpoint; retry never fetches into/overwrites a different subject. | — |
| `SYNC-01` | P1 | two-actor | Status reports `R0`–`R6` without mutation ([Settle](../spaces/lifecycle-and-collaboration.md#settle)). | Deterministic branches for no upstream/synced/ahead/behind/diverged/unreachable/revoked. | 1. run human/JSON status.<br>2. compare refs/network writes. | Correct state and local hash; no ref/remote mutation; unreachable/revoked distinct from divergence. | — |
| `SYNC-02` | P1 | two-actor + fault | Push handles concurrent head and lost response without lost update/duplicate commit ([Remain in flight](../spaces/lifecycle-and-collaboration.md#remain-in-flight)). | Local ahead; second actor moves remote before/after accept. | 1. push in each ordering.<br>2. drop response after accept.<br>3. status/retry. | Defined success/conflict/unknown; remote never overwritten silently; status/retry converges once. | — |
| `SYNC-03` | P1 | two-actor + restart | Pull does not destructively resolve ahead/diverged and recovers before ref update ([Cancel and interrupt](../spaces/lifecycle-and-collaboration.md#cancel-and-interrupt)). | Behind/ahead/diverged; fault after fetch before ref update; concurrent local write. | 1. pull each.<br>2. terminate at barrier.<br>3. restart/status/query. | Behind updates atomically; ahead/diverged preserved/reported; no local commit loss; retry safe. | — |
| `COLLAB-01` | P2 | cli + offline | Invite remote/base/shortening variants produce the intended URL without leaking authority ([Resolve](../spaces/lifecycle-and-collaboration.md#resolve)). | Zero/one/many remotes; shortcut online/offline; recipient root. | 1. mint default, explicit, no-remote, no-shorten, env variants.<br>2. inspect requests/URL. | Correct origin/remote fragment/audience; several-remotes guidance; no shortening when disabled/offline; secrets absent from logs. | — |
| `COLLAB-02` | P1 | fault + restart | Join/claim is atomic across claim, site, registry, binding, remote setup ([Cross a boundary](../spaces/lifecycle-and-collaboration.md#cross-a-boundary)). | Open/restricted invite; faults at each stage; name collision. | 1. join/fault/restart.<br>2. retry same URL/name.<br>3. inspect authority/site/registry. | One claim/subject/registration; wrong root/collision unchanged; partial state recoverable without authority duplication. | — |
| `COLLAB-03` | P1 | hybrid + second-browser | Accountless claim survives later account link and second-device recovery ([Edge cases](../spaces/lifecycle-and-collaboration.md#edge-cases)). | Provider-free profile claims; later active account; independent device. | 1. claim/write/restart accountless.<br>2. link account and sync.<br>3. second device pull/read/write. | Claim remains tied to profile/root, retained into account once, same subject/data recovers on device two. | — |
| `COLLAB-04` | P1 | two-actor + offline | Revocation denies later remote work without falsifying retained local state ([Settle](../spaces/lifecycle-and-collaboration.md#settle)). | Claimed invite, recipient with local replica, owner and multiple services. | 1. revoke with one service offline then recover.<br>2. recipient push/pull/invite/local read/write before/after reconnect. | Publication status exact; all post-commit remote authority denied; local behavior matches explicit product rule; no resurrection. | — |

## Authoring, data, transfer, and maintenance

| ID | P | Condition | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `DATA-01` | P2 | cli | Concept and all view kinds round-trip through list/show/render ([Inventory](../cli/command-surface.md#authoring-and-data)). | Blank space; representative types/cardinalities/templates. | 1. add concept/detail/directory/label/title.<br>2. list/show human/JSON/notation.<br>3. render. | Stable names/types/kinds/templates; dynamic assert help appears; no lost fields. | — |
| `DATA-02` | P1 | hybrid | `view add --home` and `space home` are atomically visible as browser home ([Open questions](../spaces/lifecycle-and-collaboration.md#open-questions-and-verification)). | Blank and configured homes; real browser on space route. | 1. add first directory/detail.<br>2. explicit replace home.<br>3. reload/navigate/interact. | Auto-home only when blank; explicit replacement atomic; actual browser home is visible/interactive, not only renderable. | — |
| `DATA-03` | P1 | cli + two-actor | Assert/update/no-op/retract preserve untouched fields and handle concurrent claims ([Inventory](../cli/command-surface.md#authoring-and-data)). | Concept with required/optional/many; two actors. | 1. create entity.<br>2. update subset/same value/concurrently.<br>3. retract field/whole.<br>4. query. | Required validation; subset preserves others; identical no-op; many/whole retraction documented; concurrency yields defined facts. | — |
| `DATA-04` | P2 | pipe | Eval input modes and output modes agree ([Resolve](../cli/command-surface.md#resolve)). | Same notation inline/file/explicit stdin/implicit pipe. | 1. run each human/JSON/quiet/home/dry-run/no-sync variant.<br>2. compare state/output. | Semantically identical results; TTY without input is actionable; broken/empty stdin commits nothing. | — |
| `DATA-05` | P2 | pipe + fault | Render/blob/export outputs handle missing targets and output failure safely ([Inventory](../cli/command-surface.md#blob-transfer-migration-telemetry-and-update)). | Valid/missing routes/blobs; writable/read-only/full output; broken pipe. | 1. output to stdout/file.<br>2. inject each failure.<br>3. inspect existing file/state. | Valid bytes/content; missing target specific; output file atomic or documented; no corrupted prior file or repeated mutation. | — |
| `DATA-06` | P1 | restart | Import is row-safe and retryable after malformed/partial failure ([Cross a boundary](../cli/command-surface.md#cross-a-boundary)). | CSV with valid rows around malformed/duplicate row; fail after each commit. | 1. import/fault/restart.<br>2. query and retry. | Atomic-all or explicit per-row checkpoint contract; no silent duplicates/loss; output names completed rows. | — |
| `DATA-07` | P2 | pipe | Space AGENTS claim round-trips default file, named file, and stdin ([Inventory](../cli/command-surface.md#space-setup-and-lifecycle)). | Blank/existing claim; Markdown with Unicode/empty/large input. | 1. set each input.<br>2. get human/JSON.<br>3. dry-run/fail push. | Exact Markdown/revision; invalid I/O safe; local commit/push warning follows common contract. | — |
| `MAINT-01` | P1 | restart | Carry/account migrations are idempotent and crash-recoverable ([Inventory](../cli/command-surface.md#blob-transfer-migration-telemetry-and-update)). | Copy/move/cross-filesystem carry; partial legacy delegations/many spaces. | 1. fault at each copy/drain/retain/push checkpoint.<br>2. restart/re-run.<br>3. inspect source/destination/authority. | No data/delegation loss or duplicate; one authoritative home; partial push explicit. | — |
| `MAINT-02` | P2 | cli | Telemetry precedence and privacy are stable ([Interactions](../cli/command-surface.md#interactions-with-other-systems)). | Default/env/DNT/persisted/corrupt/read-only combinations. | 1. status/on/off for each.<br>2. run commands with secret-like args.<br>3. inspect capture fixture. | Effective state/reason correct; write failures explicit; only static descriptors captured, never values. | — |
| `MAINT-03` | P1 | restart + fault | Update download/verify/swap rolls back safely on interruption ([Cross a boundary](../cli/command-surface.md#cross-a-boundary)). | Install-script fixture plus npm/nix/current/new versions; fail network/signature/download/swap. | 1. update/fault at each stage.<br>2. execute binary after restart.<br>3. inspect settings/temp files. | Unsupported install defers to manager; invalid update never executes; old or new complete binary works; no partial swap; check toggles persist. | — |

## Browser routing and runtime

| ID | P | Condition | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `UI-01` | P1 | fresh-browser | Every route family mounts exactly one correct top-level element ([Resolve](../ui/routing-and-runtime.md#resolve)). | Instrument DOM lifecycle. | 1. open `/settings*`, legacy `/account*`, `/activate*`, `/`, valid/invalid space routes.<br>2. use back/forward/reload. | Exactly one account/activate/site element as specified; query retained safely; no duplicate listeners/elements. | — |
| `UI-02` | P1 | fresh-browser + offline | First/returning/offline boot has bounded visible recovery ([Remain in flight](../ui/routing-and-runtime.md#remain-in-flight)). | Empty cache then coherent returning cache; fail config/assets/worker separately. | 1. load online/offline at each state.<br>2. wait documented bound.<br>3. retry/reconnect. | No indefinite blank/progress; specific recoverable error; returning coherent cache serves local content; first offline fails clearly. | — |
| `UI-03` | P1 | two-actor | Service-worker update never mixes asset generations ([Cross a boundary](../ui/routing-and-runtime.md#cross-a-boundary)). | Hold old page/Wasm/guest loads while new SW activates. | 1. trigger update in both orderings.<br>2. navigate/interact/reload.<br>3. inspect console/network/cache. | One coherent generation runs; explicit reload/update path; no uncaught ABI/asset error or lost route. | — |
| `UI-04` | P1 | returning-browser + offline | Account gate permits provider-free/local use and blocks only authority-specific work ([Exit early](../ui/routing-and-runtime.md#exit-early)). | Root missing, provider-free root, ready account with C1/C2/C3/CX; local spaces. | 1. open Hub/local/owned/settings routes offline.<br>2. perform local and remote actions. | Local-first states enter usable shell; remote action gets precise gate/error; no generic signed-out wall. | — |
| `UI-05` | P1 | two-actor | Profile switch/revoke/delete invalidates stale DOM authority ([Remain in flight](../ui/routing-and-runtime.md#remain-in-flight)). | Two tabs/profiles/devices; delay account/content task. | 1. switch/revoke/delete in actor A.<br>2. let stale task B finish.<br>3. act/reload in B. | Stale result ignored; old authority actions unavailable; correct new profile/route/local boundary visible. | — |
| `UI-06` | P1 | returning-browser | Blank/configured/invalid home routes visibly and interactively ([Settle](../ui/routing-and-runtime.md#settle)). | Space with blank home, valid directory/detail home, missing view/entity, revoked route. | 1. navigate each.<br>2. use controls.<br>3. reload/direct-link. | Hub/default or configured home appears as specified; valid view interactive; failures visible/actionable, never empty shell. | — |
| `UI-07` | P1 | fault | Deployment misconfiguration fails closed with diagnostics ([Edge cases](../ui/routing-and-runtime.md#edge-cases)). | Wrong/missing account/access origin and service DID; capture logs/network. | 1. boot settings/content.<br>2. attempt account/space action.<br>3. correct config/reload. | Visible config/service error, no credentials sent to wrong origin, no infinite loop; corrected coherent build recovers. | — |
| `UI-08` | P2 | browser | Async custom element disconnect cannot repaint a later route/profile ([Cancel and interrupt](../ui/routing-and-runtime.md#cancel-and-interrupt)). | Delay account/activate/site response. | 1. navigate away/disconnect.<br>2. resolve old response.<br>3. inspect DOM/console/state. | Old task may have durable feature result, but never mutates new element/route UI; no uncaught error/listener leak. | — |
| `UI-09` | P2 | keyboard + touch | Busy/errors/confirmations are keyboard, touch, and screen-reader usable ([Interactions](../ui/routing-and-runtime.md#interactions-with-other-systems)). | Settings/activation/Hub in normal/error/busy/confirmation states. | 1. use keyboard only/touch.<br>2. inspect accessibility tree/live regions/focus.<br>3. zoom. | Logical focus, trapped only in modal, announced status/error, reachable cancel/retry, no hidden enabled action. | — |
| `UI-10` | P3 | browser | Compact and zoomed layouts retain the full interaction ([Edge cases](../ui/routing-and-runtime.md#edge-cases)). | 390 px viewport, 200% zoom, reduced motion. | 1. complete account choice/settings/activation/Hub navigation.<br>2. capture screenshots and overflow. | No clipped critical content/control or accidental horizontal trap; same states/actions remain available. | — |

Not checkable without a product decision:

- The post-revocation local-write contract for retained joined-space replicas.
- Whether import is atomic-all or explicitly incremental with a durable row
  checkpoint.
- Exact CLI exit-code taxonomy beyond currently pinned cancellation behavior.
- Atomic output-file guarantees for render/export and other file-producing
  commands.
- The supported service-worker update policy: immediate takeover, prompt, or
  next-navigation activation.
