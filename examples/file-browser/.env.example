# Server Configuration
PORT=6080

# API Keys (replace with your actual keys)
WEATHER_API_KEY=your_weather_api_key_here

# Add more API keys as needed
# EXAMPLE_API_KEY=your_example_api_key_here

# Tonk Relay Server Configuration
# Default relay server URL (used if not specified via UI or localStorage)
# For local development:
VITE_RELAY_URL=http://localhost:8081

# For production deployment:
# VITE_RELAY_URL=https://relay.tonk.xyz

# Or use a custom relay server:
# VITE_RELAY_URL=http://your-relay-server:8081
