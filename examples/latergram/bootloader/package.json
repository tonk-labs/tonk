{
  "name": "trail-runner",
  "version": "0.0.2",
  "description": "A boot-loader for a local-first web.",
  "scripts": {
    "dev": "npm run build:sw && vite dev",
    "build": "npm run build:sw && vite build",
    "build:sw": "vite build --config vite.sw.config.ts && cp -r dist-sw/* dist/",
    "preview": "vite preview",
    "serve-raw": "serve src"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "buffer": "^6.0.3",
    "prettier": "^2.8.8",
    "process": "^0.11.10",
    "serve": "^14.2.0",
    "util": "^0.12.5",
    "vite": "^5.0.0"
  },
  "dependencies": {}
}
