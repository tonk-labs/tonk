procs:
  tonk-app:
    shell: 'cd app && pnpm run dev'
  host-web:
    shell: 'pnpm run dev:host-web'
  relay:
    shell: '${TONK_RELAY_BINARY:-../../packages/relay/target/release/tonk-relay} 8081 bundling.tonk'
