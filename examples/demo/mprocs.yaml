procs:
  tonk-app:
    shell: "cd app && pnpm run dev"
  host-web:
    shell: "wait-port 8081 && pnpm run dev:host-web"
  relay:
    shell: "../../packages/relay/target/release/tonk-relay 8081 bundling.tonk"

