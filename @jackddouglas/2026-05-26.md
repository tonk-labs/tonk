I want to land as much of the layout manager as I can ahead of sprint planning so we have a stable surface for the next sprint. While I was implementing the specific niri layout and Linear view on Friday, many small issues cropped up that are good to track but don't meaningfully clarify or reinforce the core value prop of [[Tonk Hub]] + [[Slide]]. Many are simply regressions in Slide as the [[Tonk DSL]], [[Dialog DB]] conventions, and other parts of the system have evolved.

I'm taking it as a bad sign that both [[Gavin Owens]] and [[Irakli Gozalishvili]] could quickly vibe code prototypes of complicated layout managers, but asking the agent to do the same with our layout manager in [[Tonk Hub]] is hardly getting anywhere. Rather than push through and try to get niri-like behavior, I want to make it as simple as possible so we can just get a few views side-by-side and harden the primitives first.

The gap between what we have now and a strong MVP is largely in stability and friction, not capability.

A proper `<tonk-portal>` component that can handle arbitrary HTML/CSS/JS with the proper sandboxing in iframe is a clear next stop but not a blocker. I want to stabilize with `<tonk-display>` first, then we can revert the hacks that were implemented in `<tonk-display>` to get CSS and reactivity working properly beyond simple query replacements in the template system.

The first improvement is stable identifiers for entities, which will make the layout manager much more robust as it handles moving rows, columns, and tiles.

I'm thinking of adding a `key` field where the author can specify what elements of the concept are used to derive the entity ID. For a view, we know `model` and `name` won't change so long as the view remains the same, though other aspects of a view like its display don't fundamentally change the view, and therefore don't change the ID. Same with tiles: position doesn't fundamentally change the tile while the model it's rendering does, so we key on `model` but not `order`.

```yaml
concept!: &view
  with: { name: view-name, model: view-model, display: view-display }
  key: [model, name]          # editing `display` upserts; no duplicate (model,name)

concept!: &tile
  with: { board: .., view: .., model: .., entity: .., order: .. }
  key: [board, model, view, entity]   # order/focus are NOT in the key → mutable
```
