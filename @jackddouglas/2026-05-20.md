## IFrame `postMessage`
Finished replacing `fetch` with `MessagePort` transport, scoped down so we only care about dynamic queries at runtime, not those declared up front.

Each iframe gets a `MessageChannel`. The iframe sends a "hello" to SW on load, SW responds "ready," then they multiplex requests over the port using a unique ID per iframe.

If an iframe wants to query for all people named "Alice," it looks like this:

```ts
const rows = await globalThis.tonk.query({
  terms: { this: "?p" },
  predicate: { with: { name: "Alice" } },
});
```

The above returns an envelope containing a structured JS object like `[{this: "concept:abc...", fields: {name: "Alice"}}, ...]`.

Iframes are biased toward dynamic API use while components like `<tonk-display>` are more declarative. [[Slide]] is updated to reflect that, with a simple `tonk` API injected that exposes methods for `query`, `evaluate`, `subscribe` and `unsubscribe`.

Each method call internally awaits `tonk.ready` so agents won't get caught by boilerplate and can skip to directly querying.

## Window Manger
Reviewing [[Irakli Gozalishvili]]'s draft plan [chore: rough plan for window manager by Gozala · Pull Request #462 · tonk-labs/tonk · GitHub](https://github.com/tonk-labs/tonk/pull/462/changes#r3275019435) before continuing.

Some comments:
- `order` is a float in the spec, so inserting a column between two others splits the difference between them as the `order` for the inserted column
	- Seems easy to exhaust f64 this way
	- Strings might be better?
- How are stable `this:` identifiers generated for columns? Is it derived from the initial content then stable thereafter, in which case we don't worry about concurrency across devices?
- We would need to ignore older writes than the current optimistic state to avoid flicker while optimistic state is still source of truth for DOM
