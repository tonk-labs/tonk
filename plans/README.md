# Implementation Plans

Generated by the improve skill on 2026-07-28. Execute plans in order, honor all
STOP conditions, and update status when implementation is reviewed.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| [001](001-root-owned-account-state.md) | Finish the root-owned account state repository | P1 | L | PR #650 / `e46799bd5` (done) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with reason) | REJECTED

## Dependency notes

Plan 001 is internally divided into five reviewable PRs. They must execute in
order:

1. prove typed remote absence and atomic genesis publication;
2. add and transport the immutable root-signed descriptor;
3. mount the hidden `tonk:account` replica and trusted-base lifecycle;
4. make `AccountDisplayName` authoritative and converge projections;
5. establish descriptors and initial names for existing accounts.

The remote live gate must pass before descriptor/lifecycle implementation. The
actual cardinality-one divergence test must pass before linked rename is wired.

## Findings considered and rejected

- Reimplement the join-time `MemberName` guard: rejected because PR #650,
  present as commit `e46799bd5`, already implements and tests it.
- Store display name in the account service: rejected because the service is
  coordination/transport only, not user-state authority.
- Derive an existing account's remote from each device's current origin:
  rejected because repository identity does not locate one shared history.
- Initialize after any failed pull: rejected because outage, authorization
  failure, malformed response, and confirmed absence are not equivalent.
- Add a generic settings document/projector registry: rejected because V1 has
  one typed account fact and no demonstrated generic policy.
