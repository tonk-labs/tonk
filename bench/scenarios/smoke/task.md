You are working in a directory that is a tonk site, already connected
to its remote. Use the tonk CLI (run `tonk guide` to learn it).

Create a concept named `note` with fields `title` (string) and `body`
(string). Assert two notes: "Hello" / "First note from the bench
harness." and "World" / "Second note.". Create a view named `notes`
over the `note` concept showing title and body.

Stop when `tonk status` reports the branch is synced.
